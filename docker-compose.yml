version: "3.8"

services:
  hiab-app:
    build:
      context: .
      args:
        - VITE_GOOGLE_MAPS_API_KEY=${VITE_GOOGLE_MAPS_API_KEY}
    container_name: hiab-site-planner
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
